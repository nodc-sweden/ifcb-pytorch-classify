"""IFCB image classification pipeline."""

# Single source of truth for the version: hatchling reads it from here (see
# [tool.hatch.version] in pyproject.toml), and provenance reads it directly
# rather than through importlib.metadata, which reports what was *installed* and
# goes stale in an editable checkout.
__version__ = "0.3.0"
